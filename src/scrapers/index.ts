import * as placeHolderCarrier from './placeholderCarrier';
import * as mockIndemnity from './mockIndemnity';
const scrapers = {
  'PLACEHOLDER_CARRIER': placeHolderCarrier,
  'MOCK_INDEMNITY': mockIndemnity
}
export default scrapers;
